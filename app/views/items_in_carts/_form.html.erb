<%= form_for(@items_in_cart) do |f| %>
  <% if @items_in_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@items_in_cart.errors.count, "error") %> prohibited this items_in_cart from being saved:</h2>

      <ul>
      <% @items_in_cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br>
    <%= f.text_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
