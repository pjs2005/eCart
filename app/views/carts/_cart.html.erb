<h2>Your Cart</h2>
<table>
<%= render (cart.items_in_carts) %>
<tr>
<td colspan="2">Total</td>
<td class="grand_total"><%= number_to_currency(cart.total_price) %></td>
</tr>
</table>
<%= button_to 'Empty Cart', cart, method: :delete,
data: {confirm: 'Are you sure?' } %>
